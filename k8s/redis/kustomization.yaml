apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: baro-prod

resources:
  - deployment.yaml
  - service.yaml

commonLabels:
  app: redis
  component: cache

images:
  - name: redis
    newTag: 7.2-alpine

