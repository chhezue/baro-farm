apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: baro-notification
  labels:
    app: baro-notification
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: baro-notification
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
