apiVersion: v1
kind: Service
metadata:
  name: baro-notification
  labels:
    app: baro-notification
spec:
  type: ClusterIP
  selector:
    app: baro-notification
  ports:
    - port: 8080
      targetPort: 8091
      protocol: TCP
      name: http
