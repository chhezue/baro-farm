apiVersion: v1
kind: Service
metadata:
  name: baro-settlement
  namespace: baro-prod
  labels:
    app: baro-settlement
    component: app
spec:
  type: ClusterIP
  ports:
    - port: 8090
      targetPort: 8090
      protocol: TCP
      name: http
  selector:
    app: baro-settlement

