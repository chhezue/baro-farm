apiVersion: v1
kind: Service
metadata:
  name: baro-order
  labels:
    app: baro-order
spec:
  type: ClusterIP
  selector:
    app: baro-order
  ports:
    - port: 8080
      targetPort: 8083
      protocol: TCP
      name: http
