apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: baro-order
  labels:
    app: baro-order
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: baro-order
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
