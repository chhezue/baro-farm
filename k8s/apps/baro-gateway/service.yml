apiVersion: v1
kind: Service
metadata:
  name: baro-gateway
  labels:
    app: baro-gateway
spec:
  type: NodePort
  selector:
    app: baro-gateway
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30399
      protocol: TCP
      name: http
