apiVersion: v1
kind: Service
metadata:
  name: baro-ai
  namespace: baro-prod
  labels:
    app: baro-ai
    component: app
spec:
  type: ClusterIP
  ports:
    - port: 8092
      targetPort: 8092
      protocol: TCP
      name: http
  selector:
    app: baro-ai

