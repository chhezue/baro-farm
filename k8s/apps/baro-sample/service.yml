apiVersion: v1
kind: Service
metadata:
  name: baro-sample
  labels:
    app: baro-sample
spec:
  type: NodePort
  selector:
    app: baro-sample
  ports:
    - port: 8080
      targetPort: 8090
      nodePort: 30080
      protocol: TCP
      name: http
