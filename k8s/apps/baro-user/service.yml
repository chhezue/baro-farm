apiVersion: v1
kind: Service
metadata:
  name: baro-user
  labels:
    app: baro-user
spec:
  type: ClusterIP
  selector:
    app: baro-user
  ports:
    - port: 8080
      targetPort: 8087
      protocol: TCP
      name: http
