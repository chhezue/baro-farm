package com.barofarm.ai.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JpaConfig {
    // JPA 설정 제거됨 (DataSource 자동 설정 비활성화로 인해 JPA Auditing도 비활성화)
}
